@model IEnumerable<ILG_Global.BussinessLogic.ViewModels.HtmlContentVM>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Html Content</h1>

<button value="Create New" type="button" onclick="location.href = '@Url.Action("Create", "HtmlContent")'" class="btn btn-primary">
    <span>Create New</span>
</button>

<div class="card">
    <div class="card-header">
        <h1 class="card-title">Contact Us</h1>
    </div>
    <!-- /.card-header -->
    <div class="card-body p-0">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.SubTitle)
                    </th>

                    <th>
                        Status
                    </th>
                    <th>
                        Actions
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    if (item.IsEnabled == true)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.SubTitle)
                            </td>

                            <td>
                                <button type="button" data-toggle="tooltip" data-placement="top" title="Disable" onclick="location.href = '@Url.Action("ActivationToggle", "OurServices", new { id = item.HtmlContenID })'" class="btn btn-tool" data-card-widget="card-refresh" data-source="/HtmlContent/Index" data-source-selector="#card-refresh-content" data-load-on-init="false">
                                    <span class="badge bg-success">Enabled</span>
                                </button>
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.HtmlContenID }) |
                                @Html.ActionLink("Details", "Details", new { id = item.HtmlContenID }) |
                                @Html.ActionLink("Delete", "Delete", new { id = item.HtmlContenID })
                            </td>
                        </tr>

                    }
                    else
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.SubTitle)
                            </td>

                            <td>

                                <button type="button" data-toggle="tooltip" data-placement="top" title="Enable" onclick="location.href = '@Url.Action("ActivationToggle", "OurServices", new { id = item.HtmlContenID })'" class="btn btn-tool" data-card-widget="card-refresh" data-source="/HtmlContent/Index" data-source-selector="#card-refresh-content" data-load-on-init="false">
                                    <span class="badge bg-danger">Disabled</span>
                                </button>
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.HtmlContenID }) |
                                @Html.ActionLink("Details", "Details", new { id = item.HtmlContenID }) |
                                @Html.ActionLink("Delete", "Delete", new { id = item.HtmlContenID })
                            </td>
                        </tr>
                    }

                }
            </tbody>
        </table>
    </div>

</div>



